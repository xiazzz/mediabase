spring:

  datasource:
    url: jdbc:mysql://localhost:3306/media?useSSL=false
    username: root

  jpa:
    generate-ddl: true
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect

  mvc.view:
    prefix: /WEB-INF/
    suffix: .jsp

logging.level:
  root: WARN
